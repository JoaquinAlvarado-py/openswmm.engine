
.DS_Store

# Working notes not intended for version control
doc/FlashX_SWMM_crosswalk.md
doc/Verification_Implementation_Plan.md
/plans/
/tools/
docs/2D_MULTISCALE_FORMULATION_REVIEW.md
# Ignore examples/ contents (not the dir itself, so the fixture below can be
# re-included; git cannot re-include files under an excluded directory).
/examples/*
# Keep the canonical 2D fixture — tests/unit/engine/CMakeLists.txt and
# python/tests/engine/{test_2d_evap,test_surface2d_view}.py load it from here.
!/examples/2d_complete_example.inp

# Eclipse Stuff
.metadata/
.settings/

build/
build-*/
nrtests/
upload/

*_export.h
docs/html/

# add python ignore files and file types
python/tests/_artifacts/
python/build/
python/dist/
python/OpenSWMMCore.egg-info/
python/_skbuild/
__pycache__/
*.pyc
*.pyo
*.pyd
*.so
*.egg
*.egg-info
*.manifest
*.spec

# --- Local install prefix & generated docs (added 2026-08-04) ---
# The GUI consumes the engine as a prebuilt package from here; it is a build
# product, regenerated by `cmake --install build/<preset>`.
/install/
python/docs/_build/

# --- IDE ---
.idea/
.vscode/

# --- Test & study output ---
# Everything below is written by a test or benchmark run and regenerated on
# demand. It was ~100MB of untracked noise that buried real changes in
# `git status`.
*.log
test_artifacts/
python/test_artifacts/
python/tests/_smoke_out/
python/tests/engine/output/
# Trailing /* rather than /, so the tracked file below can be re-included:
# git cannot re-include anything under an excluded *directory*.
python/tests/legacy/output/*
!python/tests/legacy/output/b8_aq_probe.inp
python/tests/output_pet_parity/
tests/verification/
tests/regression_testing/p1c_parity/*
!tests/regression_testing/p1c_parity/compare.py
tests/regression_testing/p5_parity/
tests/regression_testing/results/
# Scratch run output written next to the tracked unit-test fixtures. Kept
# narrow on purpose: tests/unit/**/data also holds tracked expected-output
# .rpt files, so only the known scratch names are listed.
tests/unit/engine/data/*_out/
tests/unit/engine/data/control_runtime_add/
tests/unit/engine/data/deletion_ext/
tests/unit/engine/data/ignore_flags/
tests/unit/engine/data/quality_roundtrip/
tests/unit/engine/data/**/_*
tests/unit/engine/data/compare_legacy.rpt
tests/unit/engine/data/compare_new.out
tests/unit/engine/data/compare_new.rpt
tests/unit/engine/data/new_site_drainage.out
tests/unit/engine/data/new_site_drainage.rpt
tests/unit/engine/data/street_xsect.out
tests/unit/engine/data/street_xsect.rpt
tests/unit/engine/data/warnerr_*.rpt
tests/unit/legacy/engine/data/_storage_shapes_*
tests/unit/legacy/engine/data/hotstart/site_drainage_model.out
tests/unit/legacy/engine/data/hotstart/site_drainage_model.rpt
tests/unit/legacy/engine/data/hotstart/site_drainage_model_layout.html
tests/unit/legacy/engine/data/hotstart/site_drainage_model_save_hotstart.out
tests/unit/legacy/engine/data/hotstart/site_drainage_model_save_hotstart.rpt
/windowless_coupling_out/
# SQLite side-car files left when a GeoPackage fixture is opened WAL-mode.
*.gpkg-shm
*.gpkg-wal

# Model-load benchmark corpus. Generated on demand by
# tests/benchmarks/scripts/gen_load_bench.py (326 MB at the plan's sizes) —
# the generator is committed, its output is not. Results docs written here ARE
# committed, hence the negations.
tests/benchmarks/generated/*.inp
tests/benchmarks/generated/*.rpt
tests/benchmarks/generated/*.out
tests/benchmarks/generated/*.json
